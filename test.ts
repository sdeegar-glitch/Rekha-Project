import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const slots = await prisma.timeSlot.findMany(); console.log(slots); } main().catch(console.error).finally(() => prisma.$disconnect());
